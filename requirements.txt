# eXo-brain — project dependencies
# Python 3.12+ (minimum). CI and Dockerfile use 3.12.x; newer CPython (e.g. 3.13+) expected compatible unless noted.
# Install: pip install -r requirements.txt (from repo root)

# ── Runtime contracts + adapters (PyPI from SavinRazvan/eXo_adapters) ───────
# Provider SDKs (openai-agents, etc.) are transitive deps of exo-adapter-openai only.
exo-brain-core-contracts==0.1.1
exo-brain-adapter-sdk==0.1.1
exo-adapter-echo==0.1.1
exo-adapter-openai==0.1.1

# ── Validation & settings ─────────────────────────────────────────────────────
pydantic>=2.12.4
pydantic-settings>=2.14.1

# ── API transport ─────────────────────────────────────────────────────────────
fastapi>=0.136.3
uvicorn[standard]>=0.34.0
sse-starlette>=2.2.0
websockets>=14.0

# ── HTTP / async networking ───────────────────────────────────────────────────
httpx>=0.28.1
httpx2>=2.2.0  # Starlette 1.x TestClient (FastAPI test suite); silences httpx deprecation
httpx-sse>=0.4.3
anyio>=4.12.1

# ── Persistence ───────────────────────────────────────────────────────────────
aiosqlite>=0.22.1

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=9.0.3
pytest-asyncio>=1.3.0
pytest-cov>=7.1.0

# ── Notebooks ─────────────────────────────────────────────────────────────────
# Local dev + CI (nbconvert execute). GitHub in-browser preview uses its own nbformat/nbconvert
# stack; if preview fails with "An error occurred Using nbformat v5.10.4...", run:
#   python scripts/dev/normalize_notebooks_for_github.py
notebook>=7.5.6
nbformat>=5.10.4
nbconvert>=7.17.1
jupyter-server>=2.18.0
mistune>=3.2.1
pygments>=2.20.0
ipykernel>=7.2.0
ipython>=9.13.0
nest-asyncio>=1.6.0

# ── Type utilities ────────────────────────────────────────────────────────────
typing_extensions>=4.15.0

# ── Auth ──────────────────────────────────────────────────────────────────────
PyJWT>=2.10.1
cryptography>=44.0.0

# ── Observability (OTLP HTTP export when OTEL_EXPORTER_OTLP_* is set) ───────
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-exporter-otlp-proto-http>=1.42.1

# ── Environment / secrets ─────────────────────────────────────────────────────
python-dotenv>=1.0.0
