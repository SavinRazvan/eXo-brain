# eXo-brain — project dependencies
# Python 3.12+ (minimum). CI and Dockerfile use 3.12.x; newer CPython (e.g. 3.13+) expected compatible unless noted.
# Install: pip install -r requirements.txt (from repo root)

# ── Runtime contracts (published from SavinRazvan/eXo_adapters) ─────────────
# Local fallback: scripts/dev/install_adapter_dependencies.sh
exo-brain-core-contracts==0.1.1

# Provider SDKs ship with optional adapter wheels (requirements-adapters.txt).
# Do not add openai / openai-agents here — keep the adapter wall intact.

# ── Validation & settings ─────────────────────────────────────────────────────
pydantic>=2.12.4
pydantic-settings>=2.14.1

# ── API transport ─────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
sse-starlette>=2.2.0
websockets>=14.0

# ── HTTP / async networking ───────────────────────────────────────────────────
httpx>=0.28.1
httpx-sse>=0.4.3
anyio>=4.12.1

# ── Persistence ───────────────────────────────────────────────────────────────
aiosqlite>=0.22.1

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=9.0.2
pytest-asyncio>=1.3.0
pytest-cov>=7.1.0

# ── Notebooks ─────────────────────────────────────────────────────────────────
notebook>=7.5.4
nbformat>=5.10.4
nbconvert>=7.17.0
ipykernel>=7.2.0
ipython>=9.13.0
nest-asyncio>=1.6.0

# ── Type utilities ────────────────────────────────────────────────────────────
typing_extensions>=4.15.0

# ── Auth ──────────────────────────────────────────────────────────────────────
PyJWT>=2.10.1
cryptography>=44.0.0

# ── Observability (OTLP HTTP export when OTEL_EXPORTER_OTLP_* is set) ───────
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-exporter-otlp-proto-http>=1.42.1

# ── Environment / secrets ─────────────────────────────────────────────────────
python-dotenv>=1.0.0
