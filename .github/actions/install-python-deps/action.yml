# Composite action: install deps from PyPI (published adapter wheels).
name: Install Python dependencies
description: Install requirements.txt with adapter pin verification (PyPI only).
runs:
  using: composite
  steps:
    - shell: bash
      run: python -m pip install --upgrade pip
    - shell: bash
      run: bash scripts/dev/install_adapter_dependencies.sh
