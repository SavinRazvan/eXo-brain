__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# local working tree (PR artifacts, planning trackers, alignment audits)
.local/

# secrets — never commit (templates are OK)
.env
.env.*
!.env.template
!.env.example

# virtual environments
.exo_env/
.venv/
venv/
env/

# local runtime artifacts
.exo_data/

# coverage reports
.coverage/
.coverage
htmlcov/

# local Docker Compose overrides (see docker-compose.override.example.yml)
docker-compose.override.yml

# eXo diverse
_archive/*
_archive/
.tmp_tools/*
.tmp_tools
# Top-level eXo_adapters/ (optional sibling clone for adapter development) — always ignored.
eXo_adapters/

# Legacy local adapter trees — do not commit
packages/
packages/eXo_adapters_old/

# Local Cursor / agent IDE config — not published (keep on disk only)
.agents/
.cursor/

# private research corpus (kept off-git by maintainer policy)
_research_results/
_research_results/*
